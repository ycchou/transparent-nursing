# Cloudflare Worker：表單提交防護代理（Turnstile + 內容檢查 → 轉發 Apps Script）
# 部署見 worker-submit/README.md。
# 機密（TURNSTILE_SECRET / APPS_SCRIPT_URL / APPS_SCRIPT_SECRET）用 `wrangler secret put`，不寫這裡。
# 每日上限／最小間隔改由前端（單一裝置）處理，本 Worker 不需 D1。
name = "tn-submit"
main = "src/index.js"
compatibility_date = "2026-07-01"
